mutation CreateServiceProduct(
	$name: String!
	$slug: String!
	$productType: ID!
	$category: ID!
	$description: JSONString!
	$attributes: [AttributeValueInput!]!
	$userId: String!
	$userData: String!
) {
	productCreate(
		input: {
			name: $name
			slug: $slug
			productType: $productType
			category: $category
			description: $description
			chargeTaxes: false
			attributes: $attributes
			metadata: [{ key: "createdBy", value: $userId }, { key: "userData", value: $userData }]
		}
	) {
		product {
			id
		}
		errors {
			field
			message
		}
	}
}
